# 股票筛选器 — 能力菜单与方案讨论（v0.1）

> 状态：方案讨论中。目标：实时度要求不高的定时/按需筛选（如"短时间大幅下跌"、14:45 尾盘展示 KDJ 等）。

## 已确认

| 项 | 决定 |
|---|---|
| 实现语言 | Python + akshare（需 `pip install akshare`） |
| 扫描范围 | 自选股列表为主，保留"全市场"开关（约 5000 只） |
| 触发方式 | **待定**（Task Scheduler / DSH 会话内 Schedule / 手动） |
| 具体筛选规则 | **待定**（用户稍后提供） |
| 输出与通知 | **待定** |

## A. 数据与指标（勾选你要的）

- [ ] 实时快照：最新价、涨跌幅、今开/今高/今低、成交量额、量比、换手率
- [ ] KDJ(9,3,3)——日 K 或 5 分钟 K（计算方式已在本会话验证，见 `stock-kdj.ps1`）
- [ ] MACD / RSI / BOLL / BIAS 等（按需添加，akshare + pandas 现成）
- [ ] 分时均价线（现价 vs 均价，破线判定）
- [ ] 主力资金流（东财免费接口可拿：主力净流入、超大单/大单）
- [ ] 历史区间回看（如"过去 N 个交易日"、"距 N 日前涨跌"）

## B. 筛选规则（模板，阈值全部进 config 可调）

你的"稍后发我具体规则"落地后放这里。默认建议模板：

1. **短时间大幅下跌**
   - 较今日最高点回撤 ≥ X%（如 3%）
   - 最近 N 分钟跌幅 ≥ Y%（如 10 分钟跌 1%）
   - 可选：放量下跌（量比 ≥ Z）
2. **KDJ 条件**
   - 死叉（K 下穿 D）、低位金叉（K 上穿 D 且 K<N）
   - J 值极值（J < -? 或 J > ?）、连续 N 周期
3. **组合逻辑**：条件间 AND / OR，每只股票命中任意规则即入报告
4. **排除项**：ST、新股上市 <N 日、停牌、一字板等

## C. 触发与调度（三选一，可多档）

| 方式 | 优点 | 代价/限制 |
|---|---|---|
| 1. 手动（我随时跑） | 零配置 | 要有人记得问 |
| 2. Windows 任务计划程序，交易日 14:45（可加 11:00 / 14:00 多档） | 会话关着也跑，最持久；可弹桌面通知 | 脚本常驻本机 |
| 3. DSH 会话内 Schedule（`dsh web --patch apps/cli/config/examples/schedule/cordis.yml`） | 到点直接在对话里展示，原生集成 | web 会话必须开着；无系统级通知；需带 patch 重启 |

> 常驻轮询（盘中每 N 分钟）不在默认选项里——你已确认实时度要求不高，定时拉一次足够；若以后要盘中监控再加。

## D. 输出与通知

- [ ] Markdown 报告落盘（含时间戳，我随时帮你读/解读）
- [ ] 结果存 CSV（便于你后续自己分析）
- [ ] Windows 桌面 Toast / 声音提醒
- [ ] 邮件 / Server酱 / 钉钉机器人（需要各自配置，可选）
- [ ] 只输出命中股票 or 输出全部自选股的指标表（推荐后者，一眼看到全局）

## E. 与 DSH（本会话）的集成程度

| 级别 | 形态 |
|---|---|
| 零集成 | 报告落盘到工作区，你让我读我就读 |
| MCP 集成 | 核心函数包成 stdio MCP server，挂进 DSH 的 `@deepseek-ai/dsh-mcp-client`，我直接调用（工具名如 `mcp__stock__screen`） |
| Schedule 集成 | 用 C-3 的方式，到点在会话里让我现场跑 |

## 待你确认的清单

1. 具体筛选规则（数值与逻辑）——最关键
2. 自选股代码列表（或先放示例股，后续再改）
3. 触发方式：C 表选哪个？要不要多档时间？
4. 通知：只要报告？要桌面弹窗？
5. 是否需要交易日判断（akshare 自带交易日历，避开周末/节假日空跑）

## 技术背景（已在本会话验证，决策无关）

- 免费行情源（东财 `push2/push2his`、腾讯）约 3 秒快照级，对"14:45 看一眼"足够；akshare 封装了这些源，无需 API key
- 行情源只给 OHLC，KDJ 等指标需本地计算；本机已有与通达信 SMA 初始化一致的计算实现（`stock-kdj.ps1`）
- 会话内 `web_fetch` 单次可批量拉几十只（东财 `ulist.np` 批量接口）；全市场 5000 只扫描建议走独立脚本（无数据量限制）
- 本会话沙箱禁止子进程联网（curl 报 `SEC_E_NO_CREDENTIALS`），所以常驻轮询放会话内不可行——独立脚本由你本机直接运行，无此限制
