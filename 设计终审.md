# 股票指标跟踪提醒应用 — 设计终审稿（v1.0）

> 状态：等待用户终审确认。确认后开工。
> 定位：跟踪自选股票/ETF 的日 K KDJ-J 值，满足档位条件时在网页上提醒；**不做交易**。

## 1. 形态与页面

- 后端 **FastAPI**（Python 3.11，与数据层同语言）+ 前端**单页 HTML/JS**（无构建工具，浏览器打开即用，echarts 内嵌离线）。
- 页面三个区：
  1. **自选股表格**：代码/名称/现价/涨跌幅/J/K/D/今日最低 J/当前档位/今日各档报警状态；
  2. **今日警报流**：时间 + 股票 + 命中档位 + 当时 J/K/D/现价；
  3. **J 值近 30 日走势图**：每只票可选查看。
- 显示层：页面打开期间每 3 秒刷最新价。
- 访问方式：**仅本机 localhost**（默认端口 8000，config 可改），无密码。实例跑在"当前在线的那台机器"上，所以页面始终在本机可用。
- **报警送达方式（用户明确选择）：仅网页警报流，无桌面弹窗、无声音、无手机推送。** 后果提示：人不在页面时不会被"当下打断"，报警靠打开页面回看；收盘日报同样在页面内。
- 收盘日报 = 页面内的一个视图（任意交易日可回看，数据来自 SQLite），不是一次性推送：15:05 后打开页面即可看到当日各股收盘 J 值与触发汇总，错过时间也能补看。

## 2. 核心规则（冻结）

- 指标：**日 K KDJ(9,3,3) 前复权**的 **J 值**；盘中今日 K 线以实时价计算（已验证）。
- 档位：**J<0 / J<-5 / J<-10 / J<-15** 四档。
- 语义：每（股票 × 档位 × 交易日）最多报 1 次；当日盘中反复跌破同一档不重复报；次日（交易日）全部重置。同一轮可同时触发多档（各发一条）。
- 判定层：交易日盘中每 **3 分钟**一次；非交易时段与非交易日休眠（akshare 交易日历自动跳过周末/节假日）。
- 数据源：akshare（东财主源），失败自动重试并降级腾讯 → 新浪；免费 3 秒快照级，符合"实时度要求不高"。连续失败 10 轮（约 30 分钟）记警告日志。
- 停牌/无新数据：J 按最近收盘价计算，不会反复触发新档（自然处理）。
- 新增股票：热加载生效，从下一轮开始跟踪，不补当日早前历史。

## 3. 历史留存

- **SQLite 本机本地**：每 3 分钟存全部自选股快照（OHLC + K/D/J）+ 每次报警完整上下文（当时价格、指标、命中档位）。
- 不入库、不跨机同步；换机后从当天开始积累。要看旧历史，拷 db 文件即可。
- 不自动清理（数据量小），需要时可手动删除。

## 4. 部署与迁移

- **两台机器都安装、都自启、可同时运行**；迁移窗口期接受可能收到的重复报警（宁重勿漏，不引入选主机制）。
- **GitHub 私有仓**入库内容：全部代码、`config.json`（自选股名单+阈值+端口+频率，不含密钥）、`install.ps1`、`start.ps1`、README。**不入库**：`.env`（将来 Bark key 等密钥，gitignore）、SQLite、日志。
- 迁移操作 = 另一台机器 `git clone` → `install.ps1`（装依赖 `python -m pip install akshare fastapi uvicorn` + 注册自启）→ `start.ps1`（git pull 最新代码 → 启动）。1 分钟完成。
- 配置传播：改 `config.json` → git push → 另一台 `start.ps1` 自动 pull，双机同步。
- **看门狗**：Windows 计划任务每 5 分钟检查进程，不在则自动拉起（自愈 ≤5 分钟）。
- **心跳**：交易日 10:00 / 11:00 / 14:00 / 15:00 写日志 + 页面状态栏（"系统存活，今日已检查 N 次，已报警 M 条"）。心跳是事后查账用，实时保障靠看门狗。
- 时区：程序内锁定 **Asia/Shanghai**（与系统时区无关），两台机器均为北京时间。
- 日志：滚动保留 90 天。
- 风险边界：私有仓本身安全，但密钥类永不进 git 历史（防仓库误转公开/权限误配）；建议 GitHub 账号开 2FA。

## 5. 配置

- `config.json` 热加载（判定循环每轮检测变化），修改方式：对话里让我改文件。
- 初始自选股（Q23 确认，先跑通再扩）：**601138 工业富联、002475 立讯精密、300308 中际旭创**。

## 6. 边界声明

- 与 DSH（本会话）**零依赖**，独立运行；MCP 桥为后续可选件，不影响核心。
- 项目代码位置：`D:\Notes\stock-screener\`，本地 git 仓库；GitHub remote 由用户提供或自行配置。
- 明确不做：任何买入/卖出建议与执行、桌面弹窗推送、公网暴露。

## 7. 已记录的实施备注（非决策）

- Python 3.11.15 已装；pip 需用 `python -m pip`；akshare 待安装。
- KDJ 计算实现与通达信 SMA(X,N,1) 初始化一致（已有 `stock-kdj.ps1` 参照）。
- 免费行情源间歇性抽风是常态，多源降级是必须项（本会话已实测东财/腾讯互为备份）。
